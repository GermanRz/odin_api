Options -Indexes
RewriteEngine On

# Si el archivo o directorio solicitado no existe físicamente, enrutar a index.php pasando la ruta
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?ruta=$1 [QSA,L]
